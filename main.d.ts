/**
 * JSONR - A command-line HTTP client for testing APIs with support for .http files.
 *
 * This module provides:
 * - HTTP request execution from .http files
 * - Command-line argument parsing
 * - Version and help commands
 * - Request logging and debugging
 *
 * @example
 * ```bash
 * # Show version
 * jsonr --version
 *
 * # Show help
 * jsonr --help
 *
 * # Send HTTP request
 * jsonr --file request.http
 * ```
 */

/**
 * No exports - this module executes commands when imported/executed.
 */
export {};